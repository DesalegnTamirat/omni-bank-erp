<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">
        <record id="function_generate_detail_employee_attendance_report" model="ir.actions.server">
            <field name="name">Init SQL Function generate_detail_employee_attendance_report</field>
            <field name="model_id" ref="base.model_res_partner"/>
            <field name="state">code</field>
            <field name="code"><![CDATA[
# High performance PostgreSQL function for Bunna Bank attendance reporting
sql = """
CREATE OR REPLACE FUNCTION generate_daily_employee_attendance_detail_report(
    p_from DATE,
    p_to DATE,
    p_report_type VARCHAR
) RETURNS VOID AS $$
BEGIN
    -- Clear previous results for clean report generation
    TRUNCATE TABLE generate_employee_attendance_details;

    IF p_report_type = 'attendance_summary' THEN
        INSERT INTO generate_employee_attendance_details (
            employee_id,
            employee_identification,
            employee_name,
            place_of_assignment,
            work_unit,
            job_title,
            worked_hours,
            late_time_hour,
            early_exit_hour,
            over_time_hour,
            pre_defined,
            acknowledged_lateness,
            pre_approved_early_checkout,
            acknowledged_early_checkout,
            force_checkout,
            working_hours,
            attendance_percentage,
            absent_hours,
            create_date,
            write_date
        )
        SELECT 
            emp.id AS employee_id,
            COALESCE(emp.employee_identification, '') AS employee_identification,
            COALESCE(emp.name, '') AS employee_name,
            COALESCE(ou.name, '') AS place_of_assignment,
            COALESCE(ou.name, '') AS work_unit,
            CASE 
                WHEN pg_typeof(job.name)::text = 'jsonb' THEN COALESCE(job.name->>'en_US', job.name::text, '')
                ELSE COALESCE(job.name::text, '')
            END AS job_title,
            COALESCE(SUM(att.worked_hours), 0.0) AS worked_hours,
            COALESCE(SUM(att.late_time_hour), 0.0) AS late_time_hour,
            COALESCE(SUM(att.early_exit_hour), 0.0) AS early_exit_hour,
            COALESCE(SUM(att.over_time_hour), 0.0) AS over_time_hour,
            COALESCE(SUM(att.pre_defined_lateness), 0.0) AS pre_defined,
            COALESCE(SUM(att.acknowledged_late), 0.0) AS acknowledged_lateness,
            COALESCE(SUM(att.pre_approved_early_checkout), 0.0) AS pre_approved_early_checkout,
            COALESCE(SUM(att.acknowledged_exit), 0.0) AS acknowledged_early_checkout,
            COUNT(CASE WHEN att.check_out_status = 'Force Checkout' OR att.is_force_checkout = TRUE THEN 1 END) AS force_checkout,
            COALESCE(exp_wh.total_wh, 0.0) AS working_hours,
            CASE 
                WHEN COALESCE(exp_wh.total_wh, 0.0) > 0 THEN 
                    ROUND(CAST((COALESCE(SUM(att.worked_hours), 0.0) / exp_wh.total_wh) * 100 AS numeric), 2)
                ELSE 0.0 
            END AS attendance_percentage,
            GREATEST(0.0, COALESCE(exp_wh.total_wh, 0.0) - COALESCE(SUM(att.worked_hours), 0.0)) AS absent_hours,
            NOW(),
            NOW()
        FROM hr_employee emp
        LEFT JOIN operating_unit ou ON emp.default_operating_unit_id = ou.id
        LEFT JOIN hr_job job ON emp.job_position = job.id
        LEFT JOIN hr_attendance att ON att.employee_id = emp.id 
            AND att.check_in >= p_from::timestamp 
            AND att.check_in <= (p_to + interval '1 day')::timestamp
            AND (att.active = TRUE OR att.active IS NULL)
        LEFT JOIN LATERAL (
            SELECT SUM(
                COALESCE(
                    (
                        SELECT 0.0
                        FROM hr_leave l
                        WHERE l.employee_id = emp.id
                          AND l.state = 'validate'
                          AND l.date_from::date <= d::date
                          AND l.date_to::date >= d::date
                        LIMIT 1
                    ),
                    (
                        SELECT CASE 
                            WHEN rl.schedule_type = 'day_off' THEN 0.0
                            WHEN js.id IS NOT NULL THEN 
                                GREATEST(0.0, (js.end_time - js.start_time) - CASE WHEN js.has_lunch_break THEN COALESCE(js.lunch_duration, 1.0) ELSE 0.0 END)
                            ELSE 8.0
                        END
                        FROM job_position_roster_exception_line rl
                        JOIN job_position_roster_exception re ON rl.roster_id = re.id
                        LEFT JOIN job_shift js ON rl.shift_id = js.id
                        WHERE (rl.employee_id = emp.id OR re.employee_id = emp.id)
                          AND re.status = 'active'
                          AND (re.active = TRUE OR re.active IS NULL)
                          AND rl.date = d::date
                        ORDER BY re.start_date DESC, re.id DESC
                        LIMIT 1
                    ),
                    (
                        SELECT GREATEST(0.0, (js.end_time - js.start_time) - CASE WHEN js.has_lunch_break THEN COALESCE(js.lunch_duration, 1.0) ELSE 0.0 END)
                        FROM job_position_exception jpe
                        JOIN job_shift js ON jpe.shift_id = js.id
                        WHERE jpe.employee_id = emp.id
                          AND jpe.status = 'active'
                          AND (jpe.active = TRUE OR jpe.active IS NULL)
                          AND jpe.start_date <= d::date
                          AND (jpe.end_date IS NULL OR jpe.end_date >= d::date)
                        ORDER BY jpe.start_date DESC, jpe.id DESC
                        LIMIT 1
                    ),
                    (
                        SELECT GREATEST(0.0, (COALESCE(js.end_time, lbe.end_time, 17.0) - COALESCE(js.start_time, lbe.start_time, 8.0)) - CASE WHEN COALESCE(js.has_lunch_break, lbe.has_lunch_break, FALSE) THEN COALESCE(js.lunch_duration, 1.0) ELSE 0.0 END)
                        FROM location_based_exception lbe
                        LEFT JOIN job_shift js ON lbe.shift_id = js.id
                        WHERE (lbe.active = TRUE OR lbe.active IS NULL)
                          AND (lbe.operating_unit = emp.default_operating_unit_id OR lbe.id IN (
                              SELECT location_exception_id FROM rel_location_exception_operating_unit WHERE operating_unit_id = emp.default_operating_unit_id
                          ))
                          AND lbe.start_date <= d::date
                          AND (lbe.end_date IS NULL OR lbe.end_date >= d::date)
                        LIMIT 1
                    ),
                    CASE 
                        WHEN EXTRACT(DOW FROM d) BETWEEN 1 AND 5 THEN 8.0
                        WHEN EXTRACT(DOW FROM d) = 6 THEN (CASE WHEN ou.work_unit_type IN ('head_office', 'district') THEN 4.0 ELSE 8.0 END)
                        ELSE 0.0
                    END
                )
            ) AS total_wh
            FROM generate_series(p_from, p_to, '1 day'::interval) d
        ) exp_wh ON TRUE
        WHERE emp.active = TRUE
        GROUP BY emp.id, emp.employee_identification, emp.name, ou.name, ou.work_unit_type, job.id, job.name, exp_wh.total_wh;
    ELSE
        -- Daily Employee Attendance Detail mode (per-attendance row)
        INSERT INTO generate_employee_attendance_details (
            employee_id,
            employee_identification,
            employee_name,
            job_title,
            job_grade,
            place_of_assignment,
            work_unit,
            manager_name,
            attendance_month,
            checkin_date,
            checkin_time,
            checkout_time,
            remarks,
            worked_hours,
            late_time_hour,
            early_exit_hour,
            over_time_hour,
            pre_defined,
            acknowledged_lateness,
            pre_approved_early_checkout,
            acknowledged_early_checkout,
            force_checkout,
            working_hours,
            create_date,
            write_date
        )
        SELECT 
            emp.id AS employee_id,
            COALESCE(emp.employee_identification, '') AS employee_identification,
            COALESCE(emp.name, '') AS employee_name,
            COALESCE(job.name, '') AS job_title,
            COALESCE(CAST(emp.job_position AS VARCHAR), 'XIV') AS job_grade,
            COALESCE(ou.name, '') AS place_of_assignment,
            COALESCE(ou.name, '') AS work_unit,
            COALESCE(mgr.name, '') AS manager_name,
            TO_CHAR(att.check_in, 'Mon-YYYY') AS attendance_month,
            att.check_in::date AS checkin_date,
            att.check_in AS checkin_time,
            att.check_out AS checkout_time,
            COALESCE(att.check_in_status, att.check_out_status, '') AS remarks,
            COALESCE(att.worked_hours, 0.0) AS worked_hours,
            COALESCE(att.late_time_hour, 0.0) AS late_time_hour,
            COALESCE(att.early_exit_hour, 0.0) AS early_exit_hour,
            COALESCE(att.over_time_hour, 0.0) AS over_time_hour,
            COALESCE(att.pre_defined_lateness, 0.0) AS pre_defined,
            COALESCE(att.acknowledged_late, 0.0) AS acknowledged_lateness,
            COALESCE(att.pre_approved_early_checkout, 0.0) AS pre_approved_early_checkout,
            COALESCE(att.acknowledged_exit, 0.0) AS acknowledged_early_checkout,
            CASE WHEN att.check_out_status = 'Force Checkout' OR att.is_force_checkout = TRUE THEN 1 ELSE 0 END AS force_checkout,
            COALESCE(att.worked_hours, 0.0) AS working_hours,
            NOW(),
            NOW()
        FROM hr_attendance att
        JOIN hr_employee emp ON att.employee_id = emp.id
        LEFT JOIN operating_unit ou ON emp.default_operating_unit_id = ou.id
        LEFT JOIN hr_job job ON emp.job_position = job.id
        LEFT JOIN hr_employee mgr ON emp.parent_id = mgr.id
        WHERE att.check_in >= p_from::timestamp 
          AND att.check_in <= (p_to + interval '1 day')::timestamp
          AND (att.active = TRUE OR att.active IS NULL)
          AND emp.active = TRUE;
    END IF;
END;
$$ LANGUAGE plpgsql;
"""
env.cr.execute(sql)
]]></field>
        </record>

        <record id="function_auto_checkout_all_employees" model="ir.actions.server">
            <field name="name">Init SQL Function auto_checkout_all_employees</field>
            <field name="model_id" ref="base.model_res_partner"/>
            <field name="state">code</field>
            <field name="code"><![CDATA[
# Shift-Aware High Performance Auto-Checkout PostgreSQL Function (3-Hour Grace Period & Shift End Capping)
sql = """
CREATE OR REPLACE FUNCTION auto_checkout_all_employees()
RETURNS void
LANGUAGE plpgsql
AS $$
DECLARE
    v_grace_hours NUMERIC := 3.0;
BEGIN
    /* Read dynamic post-shift grace period from ir_config_parameter, defaulting to 3.0 hours if unconfigured */
    SELECT COALESCE(value::numeric, 3.0) INTO v_grace_hours
    FROM ir_config_parameter
    WHERE key = 'hr_attendance.post_shift_grace_hours'
    LIMIT 1;

    IF v_grace_hours IS NULL OR v_grace_hours <= 0 THEN
        v_grace_hours := 3.0;
    END IF;

    WITH resolved_shifts AS (
        SELECT 
            ha.id AS att_id,
            ha.check_in,
            COALESCE(
                js_roster.start_time,
                js_static.start_time,
                lbe.start_time,
                8.0
            ) AS resolved_start_time,
            COALESCE(
                js_roster.end_time,
                js_static.end_time,
                lbe.end_time,
                17.0
            ) AS resolved_end_time,
            (
                (
                    date_trunc('day', ha.check_in AT TIME ZONE 'UTC' AT TIME ZONE 'Africa/Addis_Ababa')
                    + (COALESCE(js_roster.start_time, js_static.start_time, lbe.start_time, 8.0)) * INTERVAL '1 hour'
                ) AT TIME ZONE 'Africa/Addis_Ababa'
            ) AT TIME ZONE 'UTC' AS shift_start_utc,
            (
                (
                    date_trunc('day', ha.check_in AT TIME ZONE 'UTC' AT TIME ZONE 'Africa/Addis_Ababa')
                    + (COALESCE(js_roster.end_time, js_static.end_time, lbe.end_time, 17.0)) * INTERVAL '1 hour'
                ) AT TIME ZONE 'Africa/Addis_Ababa'
            ) AT TIME ZONE 'UTC' AS shift_end_utc
        FROM hr_attendance ha
        JOIN hr_employee he ON ha.employee_id = he.id
        LEFT JOIN job_position_roster_exception_line rline 
            ON rline.employee_id = he.id 
           AND rline.date = (ha.check_in AT TIME ZONE 'UTC' AT TIME ZONE 'Africa/Addis_Ababa')::date
           AND rline.schedule_type = 'shift'
        LEFT JOIN job_shift js_roster ON rline.shift_id = js_roster.id
        LEFT JOIN job_position_exception jpe 
            ON jpe.employee_id = he.id 
           AND jpe.active = TRUE 
           AND jpe.status = 'active'
        LEFT JOIN job_shift js_static ON jpe.shift_id = js_static.id
        LEFT JOIN location_based_exception lbe 
            ON he.default_operating_unit_id = lbe.operating_unit 
           AND lbe.active = TRUE
        WHERE ha.check_out IS NULL
          AND (ha.active = TRUE OR ha.active IS NULL)
    )
    UPDATE hr_attendance ha
    SET 
        /* Force checkout sets check_out to exact Shift End Time */
        check_out = CASE 
            WHEN ha.check_in < rs.shift_end_utc THEN rs.shift_end_utc
            ELSE ha.check_in + INTERVAL '8 hours'
        END,

        /* Payable Worked Hours: Capped between MAX(check_in, shift_start) and MIN(check_out, shift_end) */
        worked_hours = GREATEST(0.0, ROUND(
            (
                EXTRACT(
                    EPOCH FROM (
                        LEAST(
                            CASE WHEN ha.check_in < rs.shift_end_utc THEN rs.shift_end_utc ELSE ha.check_in + INTERVAL '8 hours' END,
                            rs.shift_end_utc
                        )
                        -
                        GREATEST(ha.check_in, rs.shift_start_utc)
                    )
                ) / 3600.0
            )::numeric, 2
        )),

        check_out_status = 'Force Checkout',
        is_force_checkout = TRUE,
        write_date = NOW()
    FROM resolved_shifts rs
    WHERE ha.id = rs.att_id
      /* Dynamic Grace Period Verification: Current time must be past shift_end + v_grace_hours */
      AND NOW() >= (rs.shift_end_utc + (v_grace_hours || ' hours')::interval);

END;
$$ LANGUAGE plpgsql;
"""
env.cr.execute(sql)
]]></field>
        </record>

        <record id="function_detect_daily_employee_absences" model="ir.actions.server">
            <field name="name">Init SQL Function detect_daily_employee_absences</field>
            <field name="model_id" ref="base.model_res_partner"/>
            <field name="state">code</field>
            <field name="code"><![CDATA[
# High-Performance Daily Absence Detection PostgreSQL Function
sql = """
CREATE OR REPLACE FUNCTION detect_daily_employee_absences()
RETURNS void
LANGUAGE plpgsql
AS $$
DECLARE
    p_date DATE := (NOW() AT TIME ZONE 'Africa/Addis_Ababa')::date - INTERVAL '1 day';
BEGIN
    /*
    =========================================================
    Function: detect_daily_employee_absences()
    Purpose: Detects active employees who had no check-in and no approved leave.
             Inserts absence records into attendance_payroll_payload for payroll deduction.
    Execution: Triggered via Odoo 19 Scheduled Action daily at 09:30 AM (Addis Ababa time).
    =========================================================
    */
    INSERT INTO attendance_payroll_payload (
        employee_id,
        effective_date,
        hours,
        payload_type,
        state,
        display_name,
        create_uid,
        write_uid,
        create_date,
        write_date
    )
    SELECT 
        emp.id AS employee_id,
        p_date AS effective_date,
        8.0 AS hours,
        'absence' AS payload_type,
        'draft' AS state,
        'Absence Deduction - ' || emp.name || ' (' || p_date::text || ')' AS display_name,
        1 AS create_uid,
        1 AS write_uid,
        NOW() AS create_date,
        NOW() AS write_date
    FROM hr_employee emp
    WHERE emp.active = TRUE
      AND NOT EXISTS (
          SELECT 1 FROM hr_attendance att
          WHERE att.employee_id = emp.id
            AND (att.check_in AT TIME ZONE 'UTC' AT TIME ZONE 'Africa/Addis_Ababa')::date = p_date
            AND (att.active = TRUE OR att.active IS NULL)
      )
      AND NOT EXISTS (
          SELECT 1 FROM hr_leave l
          WHERE l.employee_id = emp.id
            AND l.state = 'validate'
            AND l.date_from::date <= p_date
            AND l.date_to::date >= p_date
      )
      AND NOT EXISTS (
          SELECT 1 FROM attendance_payroll_payload app
          WHERE app.employee_id = emp.id
            AND app.effective_date = p_date
            AND app.payload_type = 'absence'
      )
      /* Exclude Days Off (Roster Exception Line Day Off, Static Exception Day Off, or Default Sunday Day Off) */
      AND NOT EXISTS (
          SELECT 1 FROM job_position_roster_exception_line rl
          JOIN job_position_roster_exception r ON rl.roster_id = r.id
          WHERE rl.employee_id = emp.id
            AND r.active = TRUE
            AND r.status = 'active'
            AND rl.date = p_date
            AND rl.schedule_type = 'day_off'
      )
      AND NOT EXISTS (
          SELECT 1 FROM job_position_exception jpe
          WHERE jpe.employee_id = emp.id
            AND jpe.active = TRUE
            AND jpe.status = 'active'
            AND NOT EXISTS (
                SELECT 1 FROM job_position_roster_exception r2
                WHERE r2.employee_id = emp.id AND r2.active = TRUE AND r2.status = 'active' AND r2.start_date <= p_date AND r2.end_date >= p_date
            )
            AND (EXTRACT(ISODOW FROM p_date) = 7 OR jpe.day_off = p_date)
      )
      AND NOT (
          EXTRACT(ISODOW FROM p_date) = 7
          AND NOT EXISTS (
              SELECT 1 FROM job_position_roster_exception r3 WHERE r3.employee_id = emp.id AND r3.active = TRUE AND r3.status = 'active' AND r3.start_date <= p_date AND r3.end_date >= p_date
          )
          AND NOT EXISTS (
              SELECT 1 FROM job_position_exception jpe2 WHERE jpe2.employee_id = emp.id AND jpe2.active = TRUE AND jpe2.status = 'active'
          )
      );

END;
$$ LANGUAGE plpgsql;
"""
env.cr.execute(sql)
]]></field>
        </record>

        <record id="function_detect_job_abandonment_cases" model="ir.actions.server">
            <field name="name">Init SQL Function detect_job_abandonment_cases</field>
            <field name="model_id" ref="base.model_res_partner"/>
            <field name="state">code</field>
            <field name="code"><![CDATA[
# High-Performance Job Abandonment Detection PostgreSQL Function
sql = """
CREATE OR REPLACE FUNCTION detect_job_abandonment_cases()
RETURNS void
LANGUAGE plpgsql
AS $$
DECLARE
    v_offense_id INT;
    p_check_from DATE := (NOW() AT TIME ZONE 'Africa/Addis_Ababa')::date - INTERVAL '4 days';
    p_today DATE := (NOW() AT TIME ZONE 'Africa/Addis_Ababa')::date;
BEGIN
    /*
    =========================================================
    Function: detect_job_abandonment_cases()
    Purpose: Detects employees absent for 3+ consecutive working days without approved leave,
             and automatically creates a discipline case for Job Abandonment.
    Execution: Triggered via Odoo 19 Scheduled Action daily at 03:00 AM (Addis Ababa time).
    =========================================================
    */
    SELECT id INTO v_offense_id
    FROM discipline_offense
    WHERE name ILIKE '%job abandonment%' OR name ILIKE '%unexcused absence%'
    LIMIT 1;

    IF v_offense_id IS NOT NULL THEN
        INSERT INTO discipline_case (
            employee_id,
            offense_id,
            description,
            reference,
            state,
            incident_date,
            create_uid,
            write_uid,
            create_date,
            write_date
        )
        SELECT 
            emp.id AS employee_id,
            v_offense_id AS offense_id,
            'Automatically flagged by System Audit: Unexcused absence for 3+ consecutive working days.',
            'JOB-ABANDON-' || emp.id || '-' || TO_CHAR(p_today, 'YYYYMMDD'),
            'draft' AS state,
            p_today AS incident_date,
            1 AS create_uid,
            1 AS write_uid,
            NOW() AS create_date,
            NOW() AS write_date
        FROM hr_employee emp
        WHERE emp.active = TRUE
          AND NOT EXISTS (
              SELECT 1 FROM discipline_case dc
              WHERE dc.employee_id = emp.id
                AND dc.reference LIKE 'JOB-ABANDON-%'
                AND dc.state NOT IN ('revoked', 'closed')
                AND dc.incident_date >= p_check_from
          )
          AND (
              SELECT COUNT(DISTINCT (att.check_in AT TIME ZONE 'UTC' AT TIME ZONE 'Africa/Addis_Ababa')::date)
              FROM hr_attendance att
              WHERE att.employee_id = emp.id
                AND (att.check_in AT TIME ZONE 'UTC' AT TIME ZONE 'Africa/Addis_Ababa')::date >= p_check_from
                AND (att.active = TRUE OR att.active IS NULL)
          ) = 0
          AND NOT EXISTS (
              SELECT 1 FROM hr_leave l
              WHERE l.employee_id = emp.id
                AND l.state = 'validate'
                AND l.date_from::date <= p_today
                AND l.date_to::date >= p_check_from
          );
    END IF;

END;
$$ LANGUAGE plpgsql;

-- Automated Lunch and Shift-End Force Checkout PostgreSQL Engine
CREATE OR REPLACE FUNCTION process_automated_force_checkouts() RETURNS VOID AS $$
DECLARE
    p_now_eat TIMESTAMP := NOW() AT TIME ZONE 'Africa/Addis_Ababa';
    p_today_eat DATE := (NOW() AT TIME ZONE 'Africa/Addis_Ababa')::date;
BEGIN
    -- 1. LUNCH AUTO CHECKOUT ENGINE:
    -- Runs 15 mins before Session 2 check-in time for open morning sessions.
    UPDATE hr_attendance att
    SET lunch_out = p_now_eat AT TIME ZONE 'UTC',
        check_out_status = 'Auto Lunch Checkout',
        write_date = NOW()
    FROM hr_employee emp
    LEFT JOIN job_shift s ON emp.job_shift_id = s.id
    WHERE att.employee_id = emp.id
      AND att.check_in IS NOT NULL
      AND att.lunch_out IS NULL
      AND att.check_out IS NULL
      AND (att.active = TRUE OR att.active IS NULL)
      AND s.has_lunch_break = TRUE
      AND (
          (EXTRACT(HOUR FROM p_now_eat) + EXTRACT(MINUTE FROM p_now_eat)/60.0) >= 
          (s.lunch_start_time + s.lunch_duration - 0.25)
      );

    -- 2. SHIFT-END FORCE CHECKOUT ENGINE:
    -- Auto checks out unclosed sessions past shift end. Explicitly protects Night Shifts.
    UPDATE hr_attendance att
    SET check_out = (att.check_in + INTERVAL '8 hours'),
        check_out_status = 'Force Checkout',
        is_force_checkout = TRUE,
        write_date = NOW()
    FROM hr_employee emp
    LEFT JOIN job_shift s ON emp.job_shift_id = s.id
    WHERE att.employee_id = emp.id
      AND att.check_in IS NOT NULL
      AND att.check_out IS NULL
      AND (att.active = TRUE OR att.active IS NULL)
      AND (
          -- Day Shifts: Auto checkout if open 2 hours past shift end
          (COALESCE(s.is_night_shift, FALSE) = FALSE AND (p_now_eat - (att.check_in AT TIME ZONE 'Africa/Addis_Ababa')) > INTERVAL '12 hours')
          OR
          -- Night Shifts (23:00 - 07:00): Only evaluate AFTER 08:00 AM next day
          (COALESCE(s.is_night_shift, FALSE) = TRUE AND EXTRACT(HOUR FROM p_now_eat) >= 8 AND (p_now_eat - (att.check_in AT TIME ZONE 'Africa/Addis_Ababa')) > INTERVAL '10 hours')
      );
END;
$$ LANGUAGE plpgsql;
"""
env.cr.execute(sql)
]]></field>
        </record>
    </data>
</odoo>
