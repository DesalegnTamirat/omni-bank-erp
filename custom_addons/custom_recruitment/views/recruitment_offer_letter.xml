<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Tree View -->
    <record id="recruitment_offer_letter_tree" model="ir.ui.view">
        <field name="name">recruitment.offer.letter.tree</field>
        <field name="model">recruitment.offer.letter</field>
        <field name="arch" type="xml">
            <list>
                <field name="vacancy_id"/>
                <field name="candidate_score_id"/>
                <field name="offer_date"/>
                <field name="expiry_date"/>
                <field name="state" widget="badge"
                       decoration-success="state == 'accepted'"
                       decoration-warning="state == 'sent'"
                       decoration-danger="state == 'declined'"
                       decoration-muted="state == 'draft'"/>
            </list>
        </field>
    </record>

    <!-- Form View -->
    <record id="recruitment_offer_letter_form" model="ir.ui.view">
        <field name="name">recruitment.offer.letter.form</field>
        <field name="model">recruitment.offer.letter</field>
        <field name="arch" type="xml">
            <form string="Offer Letter">
                <header>
                    <button name="action_send" string="Send Offer" type="object"
                            class="oe_highlight" invisible="state != 'draft'"/>
                    <button name="action_accept" string="Accept" type="object"
                            class="oe_highlight" invisible="state != 'sent'"/>
                    <button name="action_decline" string="Decline" type="object"
                            invisible="state != 'sent'"/>
                    <field name="state" widget="statusbar"
                           statusbar_visible="draft,sent,accepted,declined"/>
                </header>
                <sheet>
                    <group>
                        <group string="Vacancy &amp; Candidate">
                            <field name="vacancy_id"/>
                            <field name="candidate_score_id"/>
                            <field name="applicant_id"/>
                            <field name="employee_id"/>
                        </group>
                        <group string="Offer Dates">
                            <field name="offer_date"/>
                            <field name="expiry_date"/>
                            <field name="response_date" invisible="state not in ('accepted', 'declined')"/>
                        </group>
                    </group>
                    <group string="Reason" invisible="state != 'declined'">
                        <field name="rejection_reason" nolabel="1"/>
                    </group>
                    <group string="Notes">
                        <field name="notes" nolabel="1"/>
                    </group>
                </sheet>
                <chatter/>
            </form>
        </field>
    </record>

    <!-- Window Action -->
    <record id="action_recruitment_offer_letter" model="ir.actions.act_window">
        <field name="name">Offer Letters</field>
        <field name="res_model">recruitment.offer.letter</field>
        <field name="view_mode">list,form</field>
        <field name="help" type="xml">
            <p class="o_view_nocontent_smiling_face">
                No offer letters yet. Create one for a selected candidate.
            </p>
        </field>
    </record>
</odoo>
