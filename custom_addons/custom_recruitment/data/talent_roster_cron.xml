<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">

        <!-- ── Talent Roster 6-Month Expiry Cron ────────────────────────────── -->
        <record id="ir_cron_expire_talent_roster" model="ir.cron">
            <field name="name">Recruitment: Auto-Expire Talent Roster Candidates (6 Months)</field>
            <field name="model_id" ref="model_talent_roster"/>
            <field name="state">code</field>
            <field name="code">model._cron_expire_talent_roster</field>
            <field name="interval_number">1</field>
            <field name="interval_type">days</field>
            <field name="active">True</field>
        </record>

    </data>
</odoo>
