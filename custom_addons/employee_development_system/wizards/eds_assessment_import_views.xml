<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>

        <record id="view_eds_assessment_import_form" model="ir.ui.view">
            <field name="name">eds.assessment.import.form</field>
            <field name="model">eds.assessment.import</field>
            <field name="arch" type="xml">
                <form string="Import Assessment Scores">
                    <sheet>
                        <div class="alert alert-info" role="alert">
                            <span class="fa fa-info-circle me-1" title="Information" aria-hidden="true"/> <b>Pre vs Post Training Assessment Score Import:</b><br/>
                            Upload proctored baseline and post-session scores in CSV format. Columns supported: <code>session_ref, employee_badge, pre_score, post_score, comments</code>.
                        </div>
                        <group>
                            <group>
                                <field name="csv_file" filename="filename"/>
                                <field name="filename" invisible="1"/>
                            </group>
                            <group>
                                <field name="session_id" placeholder="Optional fallback session"/>
                            </group>
                        </group>
                        <group string="Import Results" invisible="not result_log">
                            <field name="result_log" readonly="1"/>
                        </group>
                    </sheet>
                    <footer>
                        <button name="action_import_assessments" string="Upload &amp; Score Participants" type="object" class="oe_highlight"/>
                        <button name="action_download_template" string="Download CSV Template" type="object" class="btn-secondary"/>
                        <button string="Close" class="btn-secondary" special="cancel"/>
                    </footer>
                </form>
            </field>
        </record>

        <record id="action_eds_assessment_import" model="ir.actions.act_window">
            <field name="name">Import Assessment Scores</field>
            <field name="res_model">eds.assessment.import</field>
            <field name="view_mode">form</field>
            <field name="target">new</field>
        </record>

    </data>
</odoo>
